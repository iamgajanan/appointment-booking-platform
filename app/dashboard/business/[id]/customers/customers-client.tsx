"use client";

import Link from "next/link";
import { useMemo, useState } from "react";

type Customer = { name: string; phone: string | null; email: string | null; total: number; completed: number; cancelled: number; lastVisit: string; services: string[] };
type Props = { business: { id: string; name: string }; customers: Customer[] };

function formatDate(value: string) { return new Intl.DateTimeFormat("en-IN", { dateStyle: "medium" }).format(new Date(value)); }

export default function CustomersClient({ business, customers }: Props) {
  const [query, setQuery] = useState("");
  const filtered = useMemo(() => customers.filter((customer) => [customer.name, customer.phone ?? "", customer.email ?? "", ...customer.services].join(" ").toLowerCase().includes(query.toLowerCase())), [customers, query]);
  return <main className="min-h-screen bg-[#f7f8f5] text-[#18221d]"><header className="border-b border-[#e5e9e2] bg-white/90"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 sm:px-8 lg:px-10"><Link href="/dashboard" className="text-sm font-semibold text-[#617662]">← Back to dashboard</Link><span className="text-sm font-bold tracking-[-0.5px]">appointly customers</span></div></header><section className="mx-auto max-w-7xl px-5 py-8 sm:px-8 sm:py-10 lg:px-10"><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="mb-2 text-[11px] font-bold uppercase tracking-[0.24em] text-[#719075]">Customer relationships</p><h1 className="text-3xl font-semibold tracking-[-1.5px] sm:text-4xl">Your customers<span className="text-[#9bb89a]">.</span></h1><p className="mt-2 text-sm text-[#7d8b80]">{business.name} · {customers.length} unique customers</p></div><Link href={`/dashboard/business/${business.id}/appointments`} className="rounded-full bg-[#17221d] px-5 py-3 text-center text-sm font-semibold text-white">View appointments ↗</Link></div><div className="mb-6 rounded-3xl border border-[#e0e8dc] bg-white p-4 shadow-[0_15px_50px_rgba(50,75,55,0.04)]"><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search by name, phone, email or service..." className="w-full rounded-2xl border border-[#dfe7db] bg-[#fbfcfa] px-4 py-3 text-sm outline-none focus:border-[#91b18f]" /></div>{filtered.length === 0 ? <div className="rounded-3xl border border-dashed border-[#ccd9c8] bg-white px-6 py-16 text-center"><h2 className="text-xl font-semibold">No customers found</h2><p className="mt-2 text-sm text-[#8a978d]">Customers will appear after they book an appointment.</p></div> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map((customer) => <article key={`${customer.email}-${customer.phone}-${customer.name}`} className="rounded-3xl border border-[#e2e9df] bg-white p-5 shadow-[0_15px_50px_rgba(50,75,55,0.04)]"><div className="flex items-start justify-between gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-[#e7f2e2] text-sm font-bold text-[#638765]">{customer.name.split(" ").map((part) => part[0]).slice(0, 2).join("").toUpperCase()}</div><span className="rounded-full bg-[#edf6e9] px-3 py-1 text-[10px] font-bold uppercase tracking-[0.12em] text-[#658366]">{customer.total} visits</span></div><h2 className="mt-5 text-xl font-semibold">{customer.name}</h2><div className="mt-2 space-y-1 text-sm text-[#7d8b80]"><p>{customer.phone || "No phone number"}</p><p className="break-all">{customer.email || "No email"}</p></div><div className="mt-5 border-t border-[#edf0eb] pt-4"><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-[#9aa79b]">Services</p><p className="mt-2 text-sm text-[#536358]">{customer.services.join(", ")}</p><p className="mt-4 text-xs text-[#8a978d]">Last visit: {formatDate(customer.lastVisit)}</p></div><div className="mt-4 flex gap-2 text-[10px] font-bold uppercase tracking-[0.1em]"><span className="rounded-full bg-[#eef8e9] px-3 py-2 text-[#5d805f]">{customer.completed} completed</span><span className="rounded-full bg-[#fff1f1] px-3 py-2 text-[#a35b5b]">{customer.cancelled} cancelled</span></div></article>)}</div>}</section></main>;
}
